spring:
 data:
   mongodb:
     username: ${MONGO_USER}
     password: ${MONGO_PASSWORD}
     database: ${MONGO_DATABASE}
     port: ${MONGO_PORT}
     host: ${MONGO_HOST}

server:
 port: 8083
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka   
